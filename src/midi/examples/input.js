/*
const easymidi = require('../index.js')

const INPUT_NAME = 'YOUR_INPUT_NAME'

const input = new easymidi.Input(INPUT_NAME)

input.on('noteoff', (args) => console.log('noteoff', args.note, args.velocity, args.channel))

input.on('noteon', (args) => console.log('noteon', args.note, args.velocity, args.channel))

input.on('aftertouch', (args) => console.log('aftertouch', args.note, args.pressure, args.channel))

input.on('cc', (args) => console.log('cc', args.controller, args.value, args.channel))

input.on('program', (args) => console.log('program', args.number, args.channel))

input.on('channelpressure', (args) => console.log('channel pressure', args.pressure, args.channel))

input.on('pitch', (args) => console.log('pitch', args.value, args.channel))

input.on('position', (args) => console.log('position', args.value))

input.on('select', (args) => console.log('select', args.song))

input.on('clock', () => console.log('clock'))

input.on('start', () => console.log('start'))

input.on('continue', () => console.log('continue'))

input.on('stop', () => console.log('stop'))

input.on('reset', () => console.log('reset'))
*/
